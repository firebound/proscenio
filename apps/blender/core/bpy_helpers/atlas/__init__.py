"""Atlas packing domain (bpy-bound).

Walks scene meshes for textured materials, composes the packed image, and
reads / writes the atlas manifest. The pure MaxRects packer lives in
``core/atlas``.
"""
