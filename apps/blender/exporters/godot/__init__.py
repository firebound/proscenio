"""Godot `.proscenio` writer.

Walks Blender data and produces a JSON document that conforms to
`packages/models/schemas/proscenio.schema.json`. The Godot plugin then turns that
document into a native `.tscn`.
"""
