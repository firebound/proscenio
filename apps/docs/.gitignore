# Docusaurus build cache (framework-specific). The generic ignores -
# node_modules, build, .env.*, .DS_Store, debug logs - live in the root
# .gitignore so they apply repo-wide.
.docusaurus
.cache-loader
