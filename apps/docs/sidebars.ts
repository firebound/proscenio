import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// This runs in Node.js - Don't use client-side code here (browser APIs, JSX...)

// Three sidebars off the same docs instance, to keep the hand-authored guides,
// the codegen schema reference, and the Blender addon reference visibly separate:
//   - guidesSidebar: the hand-written docs at the docs/ root (explicit list, so
//     the generated content/ subtree never leaks in).
//   - apiSidebar: the live-rendered schema reference under content/, autogenerated.
//   - blenderSidebar: the per-panel Blender addon reference under 02-blender-addon.
const sidebars: SidebarsConfig = {
  guidesSidebar: [
    'README',
    {
      type: 'category',
      label: 'Guides',
      items: [{type: 'autogenerated', dirName: '00-guides'}],
    },
    {
      type: 'category',
      label: 'Project',
      items: [{type: 'autogenerated', dirName: '01-project'}],
    },
  ],

  apiSidebar: [{type: 'autogenerated', dirName: 'content'}],

  blenderSidebar: [{type: 'autogenerated', dirName: '02-blender-addon'}],
};

export default sidebars;
