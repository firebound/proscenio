# Helpers

Viewport authoring aids that set up Blender for 2D cutout work; none of them touch the `.proscenio`.

**Preview Camera** drops an orthographic front camera framed the way the Godot importer expects, so what you see matches the runtime framing.
