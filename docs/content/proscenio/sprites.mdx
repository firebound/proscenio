---
title: Sprites
sidebar_position: 3
---

import JSONSchemaViewer from "@theme/JSONSchemaViewer";
import schema from "../../../packages/models/schemas/proscenio.schema.json";
import {withDefs} from "@site/src/schema/withDefs";

# Sprites

The two sprite kinds a character carries, discriminated by `type`: a deformable `polygon` mesh or a `sprite_frame` spritesheet cell.

## PolygonSprite

<JSONSchemaViewer schema={withDefs(schema, "PolygonSprite")} />

## SpriteFrameSprite

<JSONSchemaViewer schema={withDefs(schema, "SpriteFrameSprite")} />
