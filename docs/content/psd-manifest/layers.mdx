---
title: Layers
sidebar_position: 2
---

import JSONSchemaViewer from "@theme/JSONSchemaViewer";
import schema from "../../../packages/models/schemas/psd_manifest.schema.json";
import {withDefs} from "@site/src/schema/with-defs";

# Layers

The layer kinds a manifest carries: a `mesh` layer (one PNG per layer) or a `sprite` layer (a group of frames, each a `FrameEntry`).

## MeshLayer

<JSONSchemaViewer schema={withDefs(schema, "MeshLayer")} />

## SpriteLayer

<JSONSchemaViewer schema={withDefs(schema, "SpriteLayer")} />

## FrameEntry

<JSONSchemaViewer schema={withDefs(schema, "FrameEntry")} />
