# Importer-composed spritesheets. Regenerable by re-running
# packages/fixtures/simple_psd/build_blend.py (which calls the addon
# importer, which composes _spritesheets/<sprite_frame>.png on the fly).
# (*.actual.proscenio test side-effects are ignored repo-wide from the root.)
_spritesheets/
