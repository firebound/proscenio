# Spec 038: Reach - TODO

Sequenced from the assessment in [STUDY.md](STUDY.md): nothing lands now - two fences with explicit triggers, one row proposed for pruning.

## Deferred

- **Krita exporter** - gate; trigger: a concrete Krita demand signal (a real artist or contributor with a Krita-based workflow asks); the port then emits the existing PNG-plus-manifest import contract, Blender side unchanged.
- **GDExtension / C# escape hatch** - gate; trigger: any of the five documented backlog triggers hit and measured (Firebound runtime-integration bottleneck, `Polygon2D` skinning over frame budget on a real game scene, live-link throughput beyond GDScript parsing, JSON parse time as import-loop pain, in-Godot round-trip authoring); until then AGENTS.md hard rule #3 stands as written.

## Dropped

- **GIMP exporter** - weaker copy of the Krita case with a smaller 2D-animation audience by the backlog's own ranking, and a future port would ride the Krita manifest pattern anyway; propose pruning the backlog row - the manifest contract, the coa_tools2 prior art, and git history carry everything needed if demand ever appears.
