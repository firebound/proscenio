# Spec 048: Photoshop document-read performance - TODO

Sequenced from the assessment in [STUDY.md](STUDY.md): one row lands now (no live session needed); three gate on a single scheduled live Photoshop session, because the multiGet descriptor can only be validated against a real PSD.

## Now

### Scope the `busy` flag off the whole tag list

- [ ] Stop passing a per-row `busy` prop. Default approach: drop the `disabled={busy}` controls entirely - renames serialize through `core.executeAsModal` ([`api/layer-rename.ts`](../../apps/photoshop/src/api/layer-rename.ts) line 42), so the disable is defensive, not a correctness gate. Remove `busy` from `TagRowProps` ([`panels/sections/tags/Row.tsx`](../../apps/photoshop/src/panels/sections/tags/Row.tsx) lines 36, 147, 154, 162, 173, 187, 196), from `TagDetails` ([`panels/sections/tags/Details.tsx`](../../apps/photoshop/src/panels/sections/tags/Details.tsx)), and from the `TagsSection` -> `TagNodeList` -> `TagNodeBranch` pass-through ([`panels/sections/TagsSection.tsx`](../../apps/photoshop/src/panels/sections/TagsSection.tsx) lines 51, 64, 86, 128).
- [ ] If a live check later shows a real double-submit hazard, instead keep the affordance but track the in-flight `layerPath` in [`useTagTree`](../../apps/photoshop/src/hooks/useTagTree.ts) (replace the `busy` boolean at line 44 / 122-133) and disable only the row whose `node.layerPath` matches - or push the disable through a `BusyContext` consumed by the leaf controls only, so a `busy` flip changes no `TagRowProps`.
- [ ] Leave `tagRowEqual` alone ([`panels/sections/tags/Row.tsx`](../../apps/photoshop/src/panels/sections/tags/Row.tsx) lines 205-229): it already compares `busy` correctly; the earlier "`tagRowEqual` fails" wording was inaccurate. If `busy` is dropped from props, remove its comparison there too.
- [ ] Verify in a normal review that one rename no longer re-renders the whole tree (the only row in this spec that needs no live Photoshop session).

## Gated - one scheduled live Photoshop session

These three share the same async read-path plumbing and the same validation surface (a real PSD); schedule them together. There is no headless harness for the multiGet descriptor.

- [ ] **Measure first.** With a real, deep document open, confirm the per-property IPC of the synchronous walk in [`adaptDocument`](../../apps/photoshop/src/api/adapt-document.ts) (line 22) is the dominant read cost before building the rewrite. The hardened DOM walk stays the correct working path meanwhile. Recover the prior IPC diagnosis from git history first (see [`deferred.md`](../deferred.md) line 33).
- [ ] **Validate the multiGet descriptor against a real PSD.** Build the `action.batchPlay` read: a multiGet over the layer stack with `extendedReference` property keys (`name`, `visible`, `layerID`, `bounds`, the group / `layerSection` signal) and `count: -1`; reuse the batchPlay precedent in [`api/ps-selection.ts`](../../apps/photoshop/src/api/ps-selection.ts) (lines 85-100) but as a read with no modal. Confirm the exact key names and the group-nesting field against the live descriptor - they vary across UXP / PS builds and are not in the type stub ([`types/uxp.d.ts`](../../apps/photoshop/src/types/uxp.d.ts) line 99).
- [ ] **Rewrite `adaptDocument` async behind a DOM-walk fallback.** Rebuild the `Layer[]` tree in JS from the flat descriptor using the group-nesting indices. Wrap the multiGet in a try; on a thrown / wrong-shape descriptor or a tree-rebuild failure, fall back to the existing synchronous DOM walk and keep it in the tree as the always-correct safety net.
- [ ] **Thread the async read through the consumers.** Make `readActiveLayerTree` ([`api/active-document.ts`](../../apps/photoshop/src/api/active-document.ts) line 24) and `readTree` ([`hooks/useTagTree.ts`](../../apps/photoshop/src/hooks/useTagTree.ts) lines 140-144) async; convert `syncOnce` (lines 56-73) to a cancellation-guarded async effect that awaits the walk, then runs the existing `elementsEqual` structural compare before `setTree`. Add a mounted / version-guard so a resolved walk from a stale `version` cannot clobber a newer tree, plus an in-flight guard (an `inFlightRef` flag) so the next poll tick early-outs while a read is still pending - the mounted/version checks only drop stale *writes*, they do not stop a second `batchPlay` / DOM walk from starting before the previous one resolves, which on a deep PSD doubles the IPC load and reintroduces the jank. Keep the `lastSyncAtRef` de-dupe (lines 60, 92). Do NOT use Suspense - it would re-suspend and unmount open dropdowns / expanded rows on every poll, defeating `buildTagTreeReusing`'s ref preservation (lines 62-64). Keep `useActiveLayerPath` ([`hooks/useActiveLayerPath.ts`](../../apps/photoshop/src/hooks/useActiveLayerPath.ts)) in step on the shared `version` trigger.
- [ ] **Pull in the shared-adaptation-per-tick / lazy-preview sibling** ([`deferred.md`](../deferred.md) line 29): adapt the document once per `version` bump and feed the tag tree, the export preview ([`panels/ProscenioTagsPanel.tsx`](../../apps/photoshop/src/panels/ProscenioTagsPanel.tsx) lines 38-42), and the active-layer consumers from one snapshot - removing the redundant second walk per event. Same async-snapshot plumbing, same live-session validation.
