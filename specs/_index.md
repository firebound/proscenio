# Spec index

Durable map of every spec: number to topic. Spec folders are pruned once the work ships - the content stays in git history; this index keeps the human-readable record so a pruned number never loses its identity. The `_` prefix sorts it above the numbered folders.

Recover a pruned spec's full text from history:

```sh
git log --all --diff-filter=A -- 'specs/NNN-*/STUDY.md'   # find the slug + add commit
git show <commit>:specs/NNN-slug/STUDY.md
```

`002` was reused for two unrelated specs (a numbering quirk that history preserves); there is no `001`.

The 027-035 wave was pruned together on 2026-06-11 (PRs #104-#113): the resolved work left the backlogs, the not-now work moved to [deferred.md](deferred.md) / [gated.md](gated.md) / [dropped.md](dropped.md), and the locked calls landed in [decisions.md](decisions.md). Per-spec PR mapping: 027 #104, 028 #105, 029 #106, 030 #107, 031 #108, 032 #109, 033 #110, 034 #111, 035 #112 / #113.

Spec 039 was pruned 2026-06-15 (PR #116): the examples open, texture, render and animate faithfully - wrapper script paths, texture import-order, the in-plane bone convention, skinned-sibling render, rest-pose geometry bake and the rest-matrix animation builder all shipped. The remaining test-infra item (point test-godot at the real baked goldens, retire the drifting hand-authored mixed_feature copy) moved to [deferred.md](deferred.md).

Spec 041 was pruned 2026-06-13 (PR #115): the plugin fixes shipped, the multiGet reader + shared-adaptation dedup moved to [deferred.md](deferred.md), the large-doc windowing to [gated.md](gated.md), and the locked calls to [decisions.md](decisions.md).

Spec 040 was retired 2026-06-13: it was a one-time automatic pass that mapped the whole product's manual-test surface, so its two outputs are living data rather than a prunable plan. The walkable surface lives in the QA Companion tool ([tools/qa-companion/checklist/](../tools/qa-companion/checklist/)); the code-read audit (`findings.md`) was triaged and retired on 2026-06-15 - real code issues to the bug backlogs, doc/help-text gaps to `backlog-docs.md`, the rest dropped. See [decisions.md](decisions.md).

Specs 043-048 (the 2026-06-16 polish sprint) were pruned together 2026-06-18 after a per-spec audit confirmed each shipped with implementing code, behavior-locking tests, and a CI step (the deferred remainders moved to [deferred.md](deferred.md) / [gated.md](gated.md), the locked calls to [decisions.md](decisions.md), and the resolved backlog issues left [backlog](backlog/index.md)). 043 outliner-selection: viewport-follow + stale-row crash guard + single native search + the shared identity-to-index resolver (PRs #119-#123, #125). 044 weight-paint-mode-sync: modal-timer mode exit, stroke-end overlay refresh, Proximity bind params, clear-empty-vgroups; override-list scroll + named snapshots stayed in the backlog. 045 skeleton-quick-armature: labels-only Quick Armature chords, picker-driven assign-action, skeleton chrome; destructive Esc cancel gated. 046 slots-list-ux: native slot `template_list` + custom-draw attachment column + attach-via-picker; the synced-CollectionProperty backing gated. 047 godot-import-verify (PRs #127-#132): texture-gated sprite region + the four baked-golden regression guards + two model-quirk doc notes; the generic eight-golden walk stays a partial in [deferred.md](deferred.md). 048 photoshop-read-perf (PR #133): async multiGet document reader behind a DOM-walk fallback + busy-flag re-render scope; the single shared snapshot per tick stayed deferred.

Spec 036 was pruned 2026-06-18 (PRs #134, #135): the UI / help / surfaces polish shipped - the two orphaned help topics (`sprite_frame_preview`, `pose_library`) re-wired behind a reverse-coverage test, Reproject UV rewritten to deterministic planar projection, the frame clamp + the `Initial frame` -> `Frame` rename, the centered-vs-origin and Reproject-purpose help, the validation issue-click unhide-and-frame with its out-of-view-layer guard, the three lying help-strings + the unified driver-axis enum, the see-also URL migration, the panel-helper consolidation, and the N-panel restructure (Pipeline first with Import / Validate / Export, the Help panel folded into the About footer, every top-level panel collapsed by default, the Element header mirroring Skeleton). The gated remainders (validator-element-rename, i18n tables, addon screenshots, docs-URL preference, joystick / slider blend) moved to [gated.md](gated.md); the dropped surfaces (subpanel drag-reorder, onion-skin overlay, the false-premise Texture-Region-hide-for-mesh) to [dropped.md](dropped.md); the locked layout / help calls to [decisions.md](decisions.md). The materials panel was reopened as a spec-sized item in [backlog](backlog/index.md), and the tooltip-copy revision's editorial half stays there too - only the stale `TOOL_SETTINGS` legend + the lying strings shipped.

Spec 049 was pruned 2026-06-18: the now-able UI/UX polish from the post-036 walks shipped across four PRs. The shared list component was finally built - a bpy-free `compute_list_filter` core plus a `ProscenioListMixin` + a `draw_select_marker` helper - and now backs the Outliner, Slots, Bones, Actions, the Weight-Paint per-bone overrides, and the new element-driver list (the four-plus consumers cleared the "third consumer" deferral 046 left in [decisions.md](decisions.md)). On top of it: Bones gained Shift/Ctrl multi-select via the per-row-marker pattern (Pose / Edit only), the active-row highlight now follows the viewport selection across the Slots and Skeleton lists too (not just the Outliner), the Weight-Paint override list moved onto a scrolling `template_list`, and the Drive-from-Bone subpanel gained an existing-driver list with a remove operator. The help popup now reflows each topic body (one paragraph string) to the popup width at draw time, retiring the hand-wrapping and carrying the editorial pass over all 31 topics that 036 had left in the backlog. The small panel fixes: the inert provenance-overlay toggle was dropped (the Edit Weights modal already drives it), the shared automesh trace params moved to the parent Mesh Generation panel so both entry points show them, and named weight snapshots (unbounded manual save points + a rolling last-3 auto history) landed on an additive sidecar field. Nothing deferred or gated - all eleven items were "now"; the locked calls landed in [decisions.md](decisions.md). The goldens stayed CI-green; the local stale models-wheel blocks them only in a dev env without a fresh `proscenio_models`.

Spec 050 was pruned 2026-06-19: the four locked authoring calls shipped. The export validator now warns when a sprite-driving bone is not in XYZ Euler (the mode Drive-from-Bone assumes), with a one-click Convert rotation to Euler operator (active bone or whole armature) in the Skeleton panel as the fix. A manual `depth_offset` (authoring-only, no schema field) feeds the writer's `z_index` before the negate, so a plane reorders without moving the object or re-importing. An Incorporate as Element operator + Element-panel button adopts a hand-authored mesh, Auto-detecting Sprite for a single quad and Mesh otherwise (resolved in `execute`, Mesh / Sprite overridable), mirroring the Create Slot shape. The sprite-origin cleanup made the PS `[origin]` sprite-only (the importer ignores + warns on a mesh layer, since a Polygon2D origin cancels at export), retired the manual `centered` toggle to a fixed internal constant, and pinned the `[origin]` -> `Sprite2D.offset` round trip with a test. Nothing deferred or gated; the locked calls landed in [decisions.md](decisions.md). The fifth original question, the Quick Armature interaction redesign (`qa-quickarm-interaction-revision`), returned to [backlog](backlog/index.md) as a `DECIDIR` item - it needs more design time.

Spec 042 was pruned 2026-06-16 (PR #117): the slot bone-follow slice shipped complete - the Bind / Unbind operators (object-parent + a Child Of constraint that cancels the bone rest + the `slot_bone` field), `create_slot` migrated off real bone-parenting, the shared resolver reading `slot_bone`, legacy bone-parent normalization on bind and unbind, and the `mixed_feature` + `slot_swap` fixtures authoring the constraint. Nothing was deferred or gated - the STUDY's four items were all "now" and all shipped; the locked calls landed in [decisions.md](decisions.md).

Specs 051-063 were scaffolded together on 2026-06-20 as a backlog-drain planning wave: the open per-domain backlogs (bugs-found, godot-importer, photoshop, ik-ergonomics, docs, code-quality, blender-6, sonarqube, and the CodeRabbit closed-PR sweep) were routed into focused thematic specs, each STUDY-first, to clear the known-work backlog without god-specs. They stay `planned` until each STUDY locks its calls and the TODO drives implementation.

Spec 053 was pruned 2026-06-20 (PR #143): the Photoshop data-integrity and resilience work shipped in full. Stop silent data loss (a filename template dropping {name}/{index} now blocks export with a PlanError instead of overwriting PNGs; the underscore-migration applier resolves by stable layer id; the Tags advanced-fields form surfaces invalid path/scale/origin/name-pattern input and blocks Apply). Import resilience (each entry, and each sprite frame, is guarded so one rejected open or stamp degrades to a per-entry warning and prior progress survives). The staleness group (the [spritesheet] token round-trips, import path resolution accepts `\`, the import button goes busy before the picker, Validate Doc-Refresh re-runs the preview, "From selection" reports with no marquee, a vanished output folder returns a stale-folder re-pick, and the tag draft resets on node identity so same-named siblings do not share it) plus the cosmetic nitpicks (stray JSX, spurious select keys). Two backlog items were resolved as decisions: the [spritesheet] item was not data loss (the planner keys sprite-frame detection on kind, never the literal token - re-scoped to cosmetic round-trip stability), and the redundant-`disabled` ternary nitpick is rejected (it is load-bearing for UXP spectrum web components under React 16). The QA Companion photoshop checklist absorbed the behavior changes; backlog-photoshop.md and the Photoshop CodeRabbit nitpicks left the backlogs.

Spec 052 was pruned 2026-06-20 (PR #144): the no-silent-failure sweep over Blender operators and export shipped in full. Export/atlas (PR1): the first export reads pixels-per-unit from the scene panel field (the redundant operator FloatProperty removed), import warns instead of silently overwriting the scene value, Apply Packed Atlas counts a sprite only on a successful UV rewrite and reports-and-cancels on a malformed manifest, the sprites writer pairs vertices with loop indices under zip(strict=True), and degenerate Constrained Delaunay holes are filtered before the with-holes output type. Operator feedback/guards (PR2): Bake Current Pose keys only the bone's active rotation channel, Quick Armature invoke seeds lock-to-front-ortho from the panel, Copy Weights cancels a zero-coverage transfer, Bake IK restores the bone selection in a finally block, action-row feedback surfaces even at log level "errors" and guards a freed armature with ReferenceError, the Drive-from-Bone shortcut uses len(bones) > 0, and the sidecar parser re-raises a descriptive ValueError on a non-numeric field. CodeRabbit also drove cross-file axis-angle export support (the rotation-mode-aware bake exposed that the writer ignored rotation_axis_angle). The QA Companion blender checklist absorbed the behavior changes (the export-dialog pixels-per-unit test moved to removed.md); the bugs-found and Blender CodeRabbit clusters left the backlogs; the verified-by-inspection coverage gaps were logged in backlog/code-quality.md.

Spec 060 was pruned 2026-06-20: the documentation-coverage pass shipped directly on main (docs do not ride a PR). Each of the three doc homes was diffed against its modules and reconciled - the eleven Blender addon pages (the validation check list and severities rebuilt from `core/validation/*`, and the two false export claims, validate-against-schema and write-next-to-the-blend, corrected), the Photoshop plugin page (the four panels, the import and migration sections, the pixels-per-unit / filename-template / forget-folder controls, and the v1/v2 manifest drift fixed to the shipped v1), and the Godot plugin page (the import surface, the mesh and sprite elements, slot routing, and the three animation track types). The architecture overview's stale planner-kind list and its phantom v1-to-v2 manifest migration were corrected in passing. backlog-docs.md was already a thin pointer from the scaffolding wave, so nothing remained to drain.

Spec 051 was pruned 2026-06-20 (PR #145): the Godot import-builder hardening shipped. The builders now guard the document shape instead of trusting it - a shared length-guarded helper parses packed 2D points (a too-short polygon or UV point becomes Vector2.ZERO rather than reading out of bounds), skinning resolves its bones before binding so an all-missing-bone mesh is left unbound rather than skeleton-bound with zero weights (which collapses it), the bind type-checks each target as a Bone2D so a slot Node2D anchor sharing a bone name is skipped, duplicate bone names keep first-wins instead of silently overwriting the lookup, the animation-target lookup is scoped so a track cannot bind to an unrelated same-named subtree, a slot default that matches no attachment warns and falls back to the first instead of hiding all, and the sprite_frame track is set to UPDATE_DISCRETE. The dead per-key interp field was dropped (the writer only ever emits constant interp on already-discrete tracks; true per-key mixing stays gated). The slot-anchor regression test gained its missing parent assertion, and a new test_builder_guards.gd pins the guards. CodeRabbit also drove the Bone2D type-check and a fix to a vacuous test branch. The fps-step schema item stays deferred (rides the next schema-touching spec); the bezier-handle fidelity gap stays gated.

Spec 054 was pruned 2026-06-20 (PR #146): the cheap code-review residue cleared. Tests that validated nothing were tightened (the weight-reproject nearest-pick, the TypeScript-emit glob, a weight-diff gained-vert case, the 3.x bone-visibility round trip, the density boundary contract); the duplicated fixture image-rewrite helper and the skinning sys.path bootstrap moved to shared homes; dead code went (the never-called weight-paint brush mirror, an unreachable fallback label, a dead atlas image-cache lookup); redundant expressions were dropped (per-operand list(), a bool() wrapper, a double negative, the emitted-TypeScript double blank line, an untyped test ClassVar); stale docstrings, run-path comments, the Python-version note, a README typo, and a help-topic sentence were fixed; the CI workflow gained a permissions block, the frozen validator dataclass fields became tuples, and the no-hard-wrap prose rule was codified. CodeRabbit added the auto-snapshot test import hoist; the documentation pass also surfaced stale v2 manifest comments (the manifest is format_version 1) and a misplaced migration-section comment, both corrected here.

Spec 055 was pruned 2026-06-20 (PR #148): the re-import contract was settled where code and docs disagreed. The Photoshop manifest re-import PRESERVES painted weights (the guide's loses-weights warning was stale) - a same-bounds re-import keeps weights and density intact, a changed-bounds re-import reprojects from the surviving sidecar, distinct from re-rig which still LOSES. The importer now reuses the existing root armature when present instead of building a fresh one every run (which orphaned the old rig and stranded any grown rig). The Godot non-destructive reimporter was a dead, unreferenced stub promising a diff/merge that never ran, so it was deleted and the docs state plainly that re-import overwrites the generated scene while a user wrapper scene survives, confirming the locked wrapper-scene-reimport call. The guide and the weight-operation matrix were rewritten to match.

Spec 056 was pruned 2026-06-20 (PR #151): IK chains became legible and editable from inside Proscenio. Non-deform control bones (.IK / .pole) no longer leak into the Godot export (a general use_deform filter in the skeleton and animation writers, pinned by a headless test). Toggle IK was renamed to a conditional Add / Remove IK Chain label resolved from the active bone's constraint state. The Skeleton panel gained a per-row chain marker and an IK chains section (tip, length, control) driven by a live per-draw scan, the curated constraint trio (chain length, keyframable influence as the IK/FK seed, pole target) plus an opt-in in-plane lock, and control bones now join a Proscenio Controls collection with a theme color (custom shape deferred). CodeRabbit drove always-on cancel feedback, a non-destructive prebend that no longer clobbers the bone rotation mode, threaded draw context, and firmer tests.

Spec 057 was pruned 2026-06-20 (PR #147): the materials work shipped only the non-speculative slice. The full inspection / config / repair panel stayed dropped (it duplicates native Blender tools). A per-element Pixel art checkbox sets the object's image-texture nodes to Closest interpolation when on and back to Linear when off; per the locked call it defaults OFF and the importer is left unchanged (it stays on Linear), since pixel art is not assumed to be the majority case. The toggle is authoring-only UI state, never written to the schema (guarded by a not-exported test).

Spec 058 was pruned 2026-06-20 (PR #149): the saturated Quick Armature modal got a mode layer. Tab cycles Draw (the current click-drag authoring, unchanged) and a new Reparent mode, with the status bar swapping to show only the active mode's chords (the automesh-authoring precedent). Reparent is viewport pick-parent - a click resolves the nearest bone tip within a screen-constant pixel radius (Y=0 XZ projection plus the shared nearest-index), a miss is a no-op with feedback, and the hit becomes the chain parent. All seven locked Quick Armature promises survive because Draw is additive; CodeRabbit drove projecting the Reparent overlay to the same Y=0 pick plane and decoupling the Esc session-state label from the chaining field (it now reads the session-authored records).

Spec 061 was pruned 2026-06-20 (PR #150): the two cheap sprite-bone-parent DRY folds landed, behavior-preserving. The slot and sprite armature resolvers collapsed into one shared resolve_target_armature(context, obj), and the in-plane bone-orientation threshold plus its direction check moved into a shared bone_in_picture_plane helper. The three larger god-file extractions (automesh_authoring, the planes material build, the bridge.py audit) stay trigger-gated. A follow-up (PR #152) adds the no-scene defensive-path test CodeRabbit asked for.

Spec 063 was pruned 2026-06-21 (PR #153): the SonarCloud analysis pipeline was fixed and the open issues cleared. Analysis moved from the inert Automatic Analysis (which ignored sonar-project.properties entirely) to a CI-based scan (Route B) - a SHA-pinned scan workflow carrying the live project key and organization, so the scanner finally honors the sources, exclusions, and multicriteria the properties always declared. That alone cleared the bulk of the 248 mapped issues (the qa-companion tool is not in sources; the schema_bindings and test trees are excluded; the Blender operator/panel S101 class-name findings are suppressed). On top of it: pytest and vitest coverage is wired so the new-code gate has data (the in-Blender writer/exporter coverage pass stays a noted overall-coverage enhancement); every GitHub Action across the workflows is pinned to a commit SHA and the docs-deploy permissions moved to the job level (eight workflow vulnerabilities); the remaining real smells were fixed (string constants, a few TypeScript cleanups); and the genuine false positives plus the at-threshold cognitive-complexity were suppressed with justifications (the list()-snapshot and dataclasses.replace patterns, test-oracle float equality, and the S3776 export/dispatch/geometry functions, the last tracked for a careful refactor in backlog/code-quality). The repo-root tests/ tree joined the per-app test exclusions, and Automatic Analysis was disabled in the SonarCloud UI so the two modes never both run. The gate passed with 100% new-code coverage.

Spec 064 was pruned 2026-06-21 (PR #154): the in-panel help and the docs site were realigned. The popup width is now derived from the wrap budget (one px-per-char constant) so the empty right band cannot return; the copy was rewritten to a tested two-tier contract (a panel ? is one shallow what-and-why paragraph with no sections, a subpanel ? is focused on its own controls, depth moved to the page); the docs mirror the panel tree exactly (every panel a page, every subpanel an H2 section) - 09-validation.md folded into pipeline#validate with a client redirect, the four thin panel pages (outliner, animation, atlas, helpers) gained the depth the popups shed, and a two-way coverage test holds _DOC_PATHS to the mirror; and every help body is one whole-string msgid routed through iface() under a per-topic context (TRANSLATIONS empty, zero visible change) so the gated i18n locale work is unblocked without a second copy pass. The locked calls landed in decisions.md and the gated i18n-locale-tables trigger records the copy-churn prerequisite is met.

Spec 037 was pruned 2026-06-24: the dual PropertyGroup-vs-Custom-Property storage collapsed to one home per per-Object field, the last 1.0.0 blocker. The classifier keys on the headless read boundary: export-read and animatable / driver-target fields (`element_type`, `hframes`, `vframes`, `frame`, `centered`, the region fields, `y_draw_order`, `is_slot`, `slot_default`, `slot_bone`) are Custom-Property-canonical - they store in the `proscenio_*` idprop the writer reads and surface in the panel through a `get`/`set` PropertyGroup proxy over that idprop (a new bpy-free `idprop_proxy` maps the enum index<->identifier); the pure-GUI fields (`pixel_art`, `material_isolated`, `exclude_from_atlas`, the Drive-from-Bone picker, `is_outliner_favorite`) stay PropertyGroup-only. `mirror.py`, `hydrate.py`, the save-pre flush, the deferred-hydrate timer, and the PG-first branch of `read_field` are deleted; `read_field` / `read_bool_flag` are idprop-only (`pg_field` argument dropped across every call site). Drive-from-Bone (and the `sprite_frame` driver bake, the slicer-preview drivers) retargeted from `proscenio.<field>` to the `["proscenio_<field>"]` idprop path, centralized behind `driver_data_path` / `is_proscenio_driver_path` in `driver_targets`; the create operator seeds the idprop before `driver_add` so the path resolves. The direction call (no migrator: pre-release, no `.blend` in the wild) dissolved the original 1.0.0 gate - the only data-destructive step and the only reason to wait both fell away; the `mouth_drive` and `mixed_feature` fixtures rebuilt with idprop-path drivers, goldens byte-unchanged (8/8), and an in-Blender `test_storage_proxy` pins the proxy round trip + frame clamp + driver path. The locked call landed in [decisions.md](decisions.md).

Spec 065 was pruned 2026-06-21 (PR #155): the Y Location draw-order authoring unified the two disconnected depth mechanisms - the importer's z_order Y-stamp and the export-only `depth_offset` float, with their two hardcoded `0.001` constants - into one authoritative integer field. `Y Location (Draw Order)` positions the object in Y (the order times a single `y_location_spacing` preference, defaulted to Blender's 0.01 `clip_start` so stacked planes clear the perspective depth buffer) and is negated straight into the Godot `z_index`; the export reads the integer, so the spacing only spreads planes in the viewport and never shifts the exported order. The field is edited in the Element panel and inline in the Outliner (dropped on a narrow panel); the Helpers panel gained the 3D-view clip range and a Re-space Planes operator; validation warns when a manual Y drag leaves its layer. The slot_swap / mixed_feature / simple_psd fixtures carry the order so the `z_index` goldens are unchanged (mixed_feature now self-loads the addon to build under factory-startup). A live design call, not a backlog drain - no deferred or gated remainder; the locked calls landed in [decisions.md](decisions.md).

Specs 066-068 were scaffolded on 2026-06-24 as a pre-v1 feature-interest wave: STUDY-first skeletons (framing, scope, real code anchors, open decisions framed) for three authoring features the team wants before the 1.0 toolset tag. 066 mesh-generation-interaction merges the gated `manual-hull-pen-tool` (an outer-contour pen-tool) with the long-deferred automesh chord/UX redesign, since both rewrite the same modal input layer; building it reopens the `manual-hull-pen-tool` gate. 067 element-individual-reimport adds a per-Element re-import to the Element panel, riding the settled spec 055 re-import contract scoped to one manifest entry. 068 quick-armature-root-bone extends spec 058's Reparent mode to seed a chain from the importer root bone and bumps the root bone default size (0.05 -> 1 unit, exposed on import). They stay `planned` until each STUDY locks its recommendations and a TODO drives implementation.

Spec 067 was pruned 2026-06-24 (PR #160): the per-Element re-import shipped. A `Re-import from PSD` button in the Element panel (the new `reimport_element` importer entry + `PROSCENIO_OT_reimport_element`) refreshes only the active Element from its source manifest entry, leaving every sibling untouched. It scopes the spec 055 contract to one entry by reusing `planes._ensure_mesh` (same-bounds keeps painted weights + automesh density, a resized layer reprojects from the sidecar) and the existing root armature, and deliberately skips the whole-figure `_anchor_meshes_at_feet` so the one Element never shifts relative to its siblings. The Element resolves to its layer by the stamped `proscenio_import_origin` (name-keyed; a stamped origin resolves only against its own layer and a miss is a warn-and-no-op, never an object-name re-match - the CodeRabbit-driven correctness fix; the object-name fallback survives only for unstamped meshes). A new per-object `PROSCENIO_IMPORT_MANIFEST` idprop (stamped resolved-to-absolute) remembers the source path for one-click reimport, with a file-picker fallback when it is gone. The per-entry dispatch was factored out of the manifest loop so `import_manifest` and `reimport_element` share it. Pinned by seven headless tests; the locked calls are in [decisions.md](decisions.md), the GUI walk is QA Companion `BL-ELEM-ROOT-07`, and a `uv.lock` pytest-specifier drift was corrected in passing. Nothing deferred or gated.

Spec 068 was pruned 2026-06-24 (PR #161): the two root-bone gaps shipped. Quick Armature now seeds its chain parent from the resolved target armature's active bone on `invoke`, so selecting the importer root, launching, and drawing a connected bone chains the first bone onto it with zero new chords (additive - no active bone leaves the first bone unparented as before, and `_create_bone` already guards a seed absent from the target's edit bones); it composes with spec 058's Reparent for mid-session retargeting. The importer root bone default length bumped 0.05 -> 1.0 (the old length was an awkward Reparent pick target) and is now configurable per import via a `root_bone_length` `FloatProperty` threaded through `import_manifest` into `build_root_armature` like `root_bone_name`; a re-import reuses the existing root in place, so the length only sizes a freshly built root and never retro-resizes an imported rig, and the non-deform root never reaches the Godot export (goldens byte-unchanged). Pinned by seven headless tests (three seed, four length); the locked calls are in [decisions.md](decisions.md), the GUI walks are QA Companion `BL-SKEL-QUICKARM-01` step 12 and the new `BL-PIPE-IMPORT-04`. No CodeRabbit findings. Nothing deferred or gated.

Spec 069 was pruned 2026-06-27 (PR #165): the Skeleton / Armature N-panel was repaginated and per-bone export exclusion shipped. The Active Armature bone list split its row by interactivity - a non-interactive connectivity icon on the left (linked / unlinked / root) replacing the redundant bone-type icon, and an interactive right cluster (a relative-parent pin that swaps filled / hollow by state, the new Godot export toggle, and the favorite star); the list sorts hierarchy or flat-alpha off a bpy-free `bone_view` and the depth indent zeroes under A-Z. A new Rig UI subpanel renders the bone-collection tree recursively (a header row plus a grouped child-button row at every depth, not just two levels), with one color control per top-level subtree (the picker is live only on top-level rows and re-tints the whole subtree via the recursive `iter_collection_bones` / `color_bone_collection`), a per-row eye + select buttons + theme-selector drawn from identical widgets so the columns align on every row, and an INFO empty-state notice when the rig has no collections instead of the panel vanishing. Per-bone export exclusion (`bone_is_exported` = `use_deform AND NOT proscenio.exclude_from_export`) routes through the Godot skeleton + animation writers so an excluded bone is skipped with its deform children reparented to the nearest exported ancestor; the panel toggle reads the combined gate and refuses a non-deform bone (already out of the export). A Drive-from-Bone driver deliberately does NOT auto-exclude its source bone (a source can be a real deform bone, so auto-excluding would silently corrupt the skeleton). Quick Armature became an Edit-mode session (enter edit once, author on `edit_bones`, restore the entry mode on exit) with reparent by native selection - right-click selects a bone and the next connected draw chains from it - which removed spec 058's Tab Draw/Reparent mode and the whole tail-tip pick subsystem (the spec 068 seed-from-active-bone survives); the launch button swaps to Exit Quick Armature while running, and the live gesture cheat-sheet is mirrored in a collapsible panel section. The shared `chord` primitive's spacing was fixed (a leading space per label + `row.alignment = "LEFT"`, since the default EXPAND split the row width and caused the "huge gap"), and the reusable interactive-tool cheat-sheet pattern was codified in `.ai/conventions/code.md`. Custom bone-shape widgets were DROPPED (a flat 2D `custom_shape` only orients correctly when every bone's roll agrees, which a real rig's do not) in favor of the native `display_type` dropdown - the `bone_widgets` module, the `assign_bone_shape` operator, and the Bone Display subpanel were removed. The QA Companion skeleton checklist absorbed the changes (the bone-row inventory, the new Rig UI and per-bone-export walks, and the rewritten Quick Armature modal walk). Nothing deferred or gated.

Spec 066 was pruned 2026-06-27 (PR #162): the automesh interactive modal gained a manual contour pen and its chord scheme was rewritten, reopening and shipping the gated `manual-hull-pen-tool`. The OUTER stage (previously a passive auto-trace plus slider) gained a Manual contour tool on the existing click-pen machine - click points to author the outer hull, close the loop, and it replaces `output.outer` (the auto-trace stays the stage default and recomputes when you switch back, so Auto<->Contour is reversible); the downstream stages triangulate the hand-authored loop exactly as the traced one, so the SIMPLE/DENSE order and the cut/fold marking are unchanged. The saturated chord scheme was replaced: bare Tab now cycles the active tool of the current stage (OUTER: Auto-trace | Manual contour; edit-outline: Extend | Cut; edit-interior: Point | Fold | Cut) and LMB always acts with the active tool, deleting the Shift/Ctrl tap-toggle and its `_mod_tap_kind` / Ctrl+Z-disambiguation hack and collapsing the NEUTRAL/DRAW split; the fixed gestures survive as distinct keys (Ctrl+Z undo, X/Z lock, 0-9 / wheel subdivisions, RMB/Enter finish, Esc cancel, Alt+click delete), and bare Tab is intercepted so Blender's Ctrl+Tab / Shift+Tab are untouched. The pure tool-cycle vocabulary lives in `core/skinning/authoring_stages.py` (`stage_tools` / `next_tool` / `default_tool` / `tool_is_pen`, unit-tested); the contour commit and the existing 24-test authoring suite are headless-green. The QA Companion automesh walks were rewritten for the Tab cycle plus the contour author (BL-MESH-INTERACTIVE-03 / -04); the locked calls are in [decisions.md](decisions.md) and the `manual-hull-pen-tool` gate is marked shipped in [gated.md](gated.md). Nothing deferred or gated; the contour pen still requires a mesh element substrate and ends at APPLY (a from-blank pen tool and persistent re-editing are logged as `mesh-pen-authoring` in [backlog/ui-feedback.md](backlog/ui-feedback.md)).

| # | Spec | Summary | Status |
| --- | --- | --- | --- |
| 000 | initial-plan | Initial plan: what Proscenio is, settled vs open decisions; drove the Phase 0 to Phase 1 work | pruned |
| 002 | reimport-merge | Godot reimport without clobbering user work (scripts, child nodes, in-editor animations) | pruned |
| 002 | spritesheet-sprite2d | Sprite2D / spritesheet render path for frame-by-frame pixel art and effect sprites | pruned |
| 003 | skinning-weights | Per-vertex skinning weights + `Polygon2D.skeleton` wiring (deformable cutout) | pruned |
| 004 | slot-system | Named attachment slots that swap one of N sprites at runtime | pruned |
| 005 | blender-authoring-panel | Blender sidebar authoring panel replacing raw Custom Properties | pruned |
| 006 | photoshop-importer | Photoshop to Blender importer (auto mesh + armature from the manifest) | pruned |
| 007 | testing-fixtures | Test fixtures: the 1-sprite-1-PNG path + real `sprite_frame` animation coverage | pruned |
| 008 | uv-animation | UV animation tracks (scrolling textures, water, gradient sweeps); stub, never greenlit | pruned |
| 009 | code-modularity | Structural-quality audit: god-modules, mixed responsibility, DRY/SRP, behavior-preserving reorg | pruned |
| 010 | photoshop-uxp-migration | Migrate the Photoshop plugin from ExtendScript JSX to UXP (React) | pruned |
| 011 | photoshop-tag-system | Explicit per-layer tag system + tagging UI (replaces name inference) | pruned |
| 012 | quick-armature-ux | Quick Armature operator UX overhaul (preview, lifecycle, Front-Ortho snap) | pruned |
| 013 | weight-paint-automesh | Weight-paint ergonomics + automesh (alpha trace); survey of 9 cutout tools | pruned |
| 014 | typed-models-codegen | Typed domain models as the source of truth + codegen + living docs | pruned |
| 015 | monorepo-packages | Repo restructure into an apps/ + packages/ split | pruned |
| 016 | blender-app-system-organization | Layer-first reorg: `_shared/` infra tier, per-system subpackages, god-module splits | pruned |
| 017 | app-cleanups | Localized Godot + docs cleanups (builder dedup, dead assets); the two apps that passed the audit | pruned |
| 018 | photoshop-web-app-layout | Re-layout the Photoshop src into api/lib/hooks/components/panels/utils (web-app shape) | pruned |
| 019 | naming-consistency | The `Element` vocabulary: mesh to Polygon2D, sprite to Sprite2D, full wire rename | pruned |
| 020 | test-coverage | Coverage lift 36% to 88.8%, Sonar gate green; host mocks + in-Blender instrumentation | pruned |
| 021 | blender-ui-audit | Reconcile UX feedback against code, per-tool audit, bucket findings into specs (discovery) | pruned |
| 022 | blender-ui-restructure | 13-panel sibling tree: flatten the root, accordion subpanels, warn-not-hide, debug_mode | pruned |
| 023 | blender-help-docs-i18n | Per-subpanel help, online doc links, Godot badge icon, i18n mechanism | pruned |
| 024 | blender-addon-preferences | Addon preferences: log level (errors/info/debug), debug_mode, Developer group | pruned |
| 025 | code-duplication | Type-2/3/4 clone audit (AST + k-gram, beyond Sonar's line scan); ~30 single-source helpers extracted across two PRs, justified divergences (N9/N12/N14/D6) kept | pruned |
| 026 | documentation-architecture | Knowledge-home map: audience-driven Docusaurus re-IA, comment/docstring routing policy (~2,900 audited), codified in `.ai/` with enforcement | pruned |
| 027 | export-correctness | Output integrity: writer respects the armature picker, whole-mesh export, validator slot-noise + CP-read fixes | pruned |
| 028 | schema-expressiveness | Format v2: appearance / track / constraint passthrough end to end, plus the migration path | pruned |
| 029 | mesh-authoring | Automesh interactive fix + mesh-generation panel gating, defaults, and manual hull | pruned |
| 030 | skinning-weight-paint | Weight-paint bind fixes, panel cleanup, and the advanced skinning toolset | pruned |
| 031 | rigging-and-posing | Quick Armature, skeleton, drivers, IK, and pose-library authoring | pruned |
| 032 | slot-attachments | Slot placement fixes + slots panel, warnings, and skin coordination | pruned |
| 033 | atlas-packing | Atlas authoring + packing heuristics + pixels-per-unit through the pipeline | pruned |
| 034 | photoshop-plugin | PS tag-system features + export-roundtrip stability | pruned |
| 035 | project-health | CI matrix, lint / type / coverage gates, fixtures, and release packaging | pruned |
| 036 | ui-help-surfaces | Editor UI polish, help/docs system, the N-panel restructure (Pipeline-first, Help-into-About); materials/onion-skin assessed out | pruned |
| 037 | storage-split | Collapse dual PG-vs-CP storage to one canonical home per field | pruned |
| 038 | reach | Additional DCC exporters (Krita, GIMP) and the GDExtension escape hatch | planned |
| 039 | example-fidelity | Example pipeline fidelity: wrapper script paths, texture import-order, in-plane bones, skinned-sibling render | pruned |
| 040 | end-to-end-verification | Automatic pass mapping the whole manual-test surface (452 items) + a code-read audit (176 findings); both now owned by the QA Companion tool (`tools/qa-companion`) | retired |
| 041 | photoshop-overhaul | Make the UXP plugin usable: null-crash fix + export-writer resilience (the 040 trigger), layerID targeting, adaptive poll, debug toggle; multiGet + dedup deferred | pruned |
| 042 | slot-bone-follow | Slot bone-follow authoring parity: a Bind Slot to Bone operator (Child Of + slot_bone), create_slot migrated off bone-parenting, resolver reads slot_bone, fixtures author the constraint | pruned |
| 043 | outliner-selection | Outliner follows the viewport selection, no crash on stale rows, single native search | pruned |
| 044 | weight-paint-mode-sync | Edit Weights tracks the weight-paint mode; expose the Proximity bind params in the panel | pruned |
| 045 | skeleton-quick-armature | Quick Armature Esc cancels properly, Animation uses the picked armature, Skeleton chrome polish | pruned |
| 046 | slots-list-ux | Native searchable slot/attachment lists (reusable list component) + attach-to-existing-slot picker | pruned |
| 047 | godot-import-verify | Verify the sprite manual-region scaling with a test + two model-quirk doc notes | pruned |
| 048 | photoshop-read-perf | Tag-list busy-flag scoping + a batchPlay multiGet document reader | pruned |
| 049 | blender-ui-polish | Shared list component (search / multi-select / scroll) + its consumers, help popup reflow + copy revision, and small panel fixes (provenance toggle, automesh params, named snapshots) | pruned |
| 050 | blender-authoring-design | Four locked authoring items: rotation-mode guard + convert-to-Euler, manual Y-depth offset, incorporate-Blender-mesh button, sprite-origin contract cleanup (Quick-Armature interaction returned to the backlog) | pruned |
| 051 | godot-importer-hardening | Defensive validation in the Godot import builders plus animation-fidelity cleanups (length-guarded parsing, weight-first bind, scoped lookups, discrete sprite_frame track) | pruned |
| 052 | blender-operator-robustness | No-silent-failure sweep over Blender operators and export (first-export pixels-per-unit, atlas no-UV, bake-pose channel, guards for malformed input and freed refs) | pruned |
| 053 | photoshop-data-integrity | Photoshop plugin data-loss and resilience (spritesheet token, id-based targeting, per-entry import guard, filename template, staleness group) | pruned |
| 054 | code-review-cleanup | Cheap residue of the CodeRabbit closed-PR sweep: test gaps, DRY, dead code, doc typos, infra hardening | pruned |
| 055 | reimport-contract | Settle what survives a re-import (PSD weights and armature into Blender, the non-destructive Godot reimporter) and align code with docs | pruned |
| 056 | ik-authoring-ergonomics | Make IK chains legible and editable in Proscenio (rename, chain markers, expose constraint props, stop the .IK export leak, Rigify-style affordances) | pruned |
| 057 | materials-panel | Materials inspection and config surface versus a low-effort pixel-art shortcut; resolve the HASHED-versus-CLIP importer default | pruned |
| 058 | quick-armature-interaction-redesign | Redesign the saturated Quick Armature modal chord scheme and fit viewport pick-parent | pruned |
| 059 | skin-coordination | Spine-style named attachment sets across slots; needs a Godot runtime layer and the format-migration path | planned |
| 060 | documentation-coverage-pass | Reconcile docs and in-panel help against the shipped surface across the three doc homes | pruned |
| 061 | blender-module-decomposition | Behavior-preserving split of the god-files (automesh_authoring, planes material) plus two sprite-bone-parent DRY folds (folds shipped; extractions trigger-gated) | pruned |
| 062 | blender-6-compatibility | Gated forward-compat: sweep raw use_nodes reads to the version-robust getattr form for Blender 6.0 | planned |
| 063 | sonarcloud-analysis-pipeline | Fix the inert SonarCloud analysis (route plus coverage wiring) and triage the 248 open issues | pruned |
| 064 | help-copy-and-docs-alignment | Realign the in-panel help and the docs site: derived popup width, tested two-tier copy contract, exact docs<->panel mirror, deepened pages, and i18n-stable strings | pruned |
| 065 | draw-order-authoring | Unify depth authoring into one Y Location (Draw Order) integer that positions the object in Y and becomes the Godot z_index; spacing preference, Helpers clip range + Re-space operator, inline Outliner column, divergence validation | pruned |
| 066 | mesh-generation-interaction | Spine-style outer-contour pen-tool (click points, close loop, live triangulation preview) plus a full redesign of the automesh modal chord scheme (bare Tab cycles the per-stage tool, replacing the tap-toggle; keeps Ctrl+Z / X-Z lock / subdivisions); reopened and shipped the gated manual-hull-pen-tool | pruned |
| 067 | element-individual-reimport | Re-import a single Element (one PSD layer) from the Element panel, honoring the spec 055 contract scoped to one entry; Element-to-manifest-entry resolution and changed/renamed/missing-layer behavior | pruned |
| 068 | quick-armature-root-bone | Retarget the authored chain onto an existing bone (the importer root), extending spec 058's Reparent; root bone default size 0.05 -> 1 unit, exposed as a configurable import operator property | pruned |
| 069 | skeleton-rig-ui-and-bone-display | Repaginate the Skeleton / Armature N-panel (recursive Rig UI tree with one color per subtree, connectivity-split bone rows, hierarchy/flat sort, Quick Armature Edit-mode session + reparent-by-selection) and add per-bone export exclusion; custom bone-shape widgets dropped for the native display_type | pruned |
