# Code-quality backlog

Cross-cutting type-safety and lint-enforcement gaps surfaced by the 2026-06-06 quality audit. Scope is narrow: the project's hard rule is strong end-to-end typing across TypeScript, JavaScript, Python, and GDScript, with no hardcoded escape hatches (`any`, blanket `# type: ignore`, unscoped `eslint-disable`, `@ts-nocheck`) and no linters bypassed in CI or pre-commit.

These entries track places where a strict gate is configured but not enforced, or where a tree is exempted from type checking entirely. Feature-shaped work lives in [`backlog.md`](backlog.md); this file is exclusively code-health and toolchain enforcement. Each entry promotes into a numbered spec under `specs/` when work begins.

The 2026-06-11 reconciliation closed two of the original holes (the ESLint CI/pre-commit gate and the `packages/{models,codegen}` mypy gate both shipped in #112) and moved a third behind a trigger (the bpy-bound `ignore_errors` sweep now lives in [`gated.md`](gated.md)). The one entry below is what remains.

## Audit baseline (what is already clean)

So the next reader does not re-audit from scratch:

- **No blanket suppressions.** Every `# type: ignore` carries a specific error code (`[import-not-found]`, `[valid-type]`, `[arg-type]`); a grep for bare `# type: ignore` returns nothing. Every `eslint-disable-next-line` is single-line with a justification. The only `@ts-nocheck` is [`apps/photoshop/src/entry.ts`](../apps/photoshop/src/entry.ts) (vendored Adobe UXP starter shim, excluded from lint, documented). The only GDScript `gdlint: ignore` is one scoped line in a test.
- **tsconfig is maximally strict.** [`apps/photoshop/tsconfig.json`](../apps/photoshop/tsconfig.json) sets `strict`, `noImplicitAny`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noPropertyAccessFromIndexSignature`, plus the unused-locals/params and `useUnknownInCatchVariables` checks.
- **mypy is strict-strict where it runs.** [`apps/blender/pyproject.toml`](../apps/blender/pyproject.toml), [`packages/validator/pyproject.toml`](../packages/validator/pyproject.toml), and now [`packages/models/pyproject.toml`](../packages/models/pyproject.toml) + [`packages/codegen/pyproject.toml`](../packages/codegen/pyproject.toml) carry `strict = true` (the models/codegen pair drops `disallow_any_explicit` under the pydantic plugin; the rest of the strict profile holds).
- **CI does not bypass gates.** No `continue-on-error`, `allow_failure`, or `|| true` anywhere in [`.github/workflows/`](../.github/workflows/). [`.pre-commit-config.yaml`](../.pre-commit-config.yaml) header states `--no-verify` is a bug. The `skipping` branches in `validate-schema` are "no fixtures present", not gate bypasses. ESLint now runs as a CI step and a pre-commit hook.

## The bundled proscenio_models wheel has no staleness gate

**What:** The Blender addon bundles a built copy of the source-of-truth model as [`apps/blender/wheels/proscenio_models-0.1.0-py3-none-any.whl`](../apps/blender/wheels/), and the writer imports `MeshElement` / `SpriteElement` / etc. from that wheel, not from `packages/models/src/`. The JSON Schema, Godot bindings, and TypeScript bindings each have a committed-match staleness test (`test_committed_schemas_match_emit`, `test_committed_artifacts_match_emit`, `test_committed_ts_bindings_match_emit`), but nothing checks that the bundled wheel reproduces from the current model. [`apps/blender/wheels/README.md`](../apps/blender/wheels/README.md) "When to bump" lists dependency and Python-ABI changes only, so a model field or shape change reads as not a wheel-rebuild trigger - even though the wheel is built locally from `packages/models/` and must be rebuilt on any shape change.

**Why it matters:** A contributor can edit `proscenio.py`, run `python -m proscenio_codegen all` (regenerating the schema, Godot, and TS artifacts - all gated), and ship a PR that looks complete while the bundled wheel still carries the old model. The Blender addon then exports the old wire shape. In PR #104 (multi-polygon export) the writer emitted the new `polygons` field, but the addon-side `MeshElement` loaded from the stale wheel rejected it with `extra_forbidden`; only a hand-written in-Blender test (`apps/blender/tests/operators/test_multi_polygon_export.py`) caught it, and that test had to self-skip when the wheel is stale rather than fail. CI installs into Blender's Python from `apps/blender/wheels/` (`ci.yml` "Install bundled wheels"), so a stale wheel silently degrades every Blender-side test against the old shape rather than going red. The wheel is the one downstream artifact of the source-of-truth model with no automated freshness check.

**Scope sketch:** Add a staleness check mirroring the schema/godot/ts ones - build `proscenio_models` to a temp dir, extract it, and assert the extracted `proscenio_models/*.py` (or the built model's `model_fields` / dumped schema) matches `packages/models/src/`. A byte-diff of the `.whl` is fragile (build timestamps and archive metadata vary run to run), so compare extracted source content or the in-memory schema, not the archive bytes. Wire it as a pytest under `tests/codegen/` and skip cleanly when the build backend is unavailable (mirroring the gdformat / npx skips elsewhere). Separately, add a `wheels/README.md` "When to bump" line: a shape change to any `packages/models/` model requires rebuilding and committing the `proscenio_models` wheel via `uv build packages/models --wheel --out-dir apps/blender/wheels`.

**Trigger to revisit:** the next `.proscenio` or PSD-manifest model field change, or when a Blender-side test passes against a stale wheel and a post-mortem asks why the shipped addon exported the old schema. Cheapest to land alongside the next schema-touching spec (schema-expressiveness), which rebuilds the wheel anyway.

## Hardcoded line breaks in prose (markdown, code comments, Blender tooltip strings)

**What:** Prose across the repo is hand-wrapped at ~72-80 columns in two places: markdown bodies and code comments. Nothing mandates this - [`.editorconfig`](../.editorconfig) sets no `max_line_length`, there is no Prettier `proseWrap` (Prettier is not in the toolchain), and [`.ai/conventions/docs.md`](../.ai/conventions/docs.md) says nothing about wrapping while being itself soft-wrapped (one paragraph per line). It is an editor/LLM default that stuck and that LLMs keep reintroducing because no rule pins it. A 2026-06-13 scan found markdown is mostly already soft-wrapped: only 8 files carry hard wraps (the `specs/040-end-to-end-verification/*` set and the hand-written `examples/generated/*/README.md` files). Code comments are the volume: ~890 wrapped continuations across ~126 files (worst: `apps/blender/operators/armature/quick_armature.py`, `apps/blender/operators/automesh/automesh_authoring.py`, `apps/photoshop/src/lib/planner.ts`).

**Why it matters:** Rendered markdown reflows to the viewport, so a source-level break is pure noise - it bloats diffs (a one-word edit reflows a whole paragraph) and reads worse in any width but the author's. The motivating symptom is Blender tooltips and in-addon doc text appearing to use only part of the available tooltip width. Note the scan did not find a source cause for that in operator descriptions: `bl_description` / `description=` strings are built by implicit string concatenation with trailing spaces (`"... image " "alpha ..."`), which Python joins into a single line that Blender auto-wraps correctly; literal `\n` escapes exist only in `apps/blender/tests/run_tests.py` (test output), and consecutive `layout.label(text=...)` paragraph-faking is minimal (one spot of note, `apps/blender/operators/skinning/edit_weights.py`). So the narrow-tooltip issue needs a concrete repro (which operator, screenshot) to localize before any string is changed - it is likely Blender's own tooltip wrap width or a specific spot, not a pervasive hardcoded-break pattern.

**Scope sketch:** The intent is to phase breaks out, not to force a manual reflow pass. (1) Codify the rule: add a no-hard-wrap line to [`docs.md`](../.ai/conventions/docs.md) ("prose is one line per paragraph/bullet; let the editor soft-wrap; never hand-wrap markdown bodies or comment paragraphs") so contributors and LLMs stop reintroducing it. (2) Reflow opportunistically: unwrap the 8 markdown files when next touched, and unwrap a comment block when already editing that file - no repo-wide rewrite right before publication. (3) Blender tooltips: capture a repro first (operator id + screenshot of the narrow tooltip), then fix that specific source, rather than mass-editing descriptions that already render fine.

**Trigger to revisit:** when a Blender tooltip or doc panel visibly wastes width and someone has the repro to localize it; otherwise unwrap in passing whenever a wrapped file is edited. A pre-commit gate for markdown hard-wrap was considered and deferred - it is hard to write without false positives on lists, tables, and code fences.

## QA-findings doc/help-text coverage pass (grouped, ~94 findings)

**What:** A 4-agent audit (2026-06-15) re-verified every row of [`tools/qa-companion/findings.md`](../tools/qa-companion/findings.md) against current `main` and bucketed each into A (doc/help-text only), B (real code issue), or C (ignorable / already fixed / by-design). The A bucket - every `undocumented`-type row plus the `drift` rows where the code is correct and only the prose is stale - groups here as one work item, because all in-addon help text and the doc pages need a review pass regardless. The A set is the remainder after removing the B and C exceptions below.

Counts by subsystem (A / B / C): Blender ~63 / 19 / ~13; Godot 10 / 9 / 1 (F-03 already fixed by #116); Photoshop 15 / 16 / 9 (F-01/F-02/F-13/F-14/F-37 already shipped by specs 041/048).

- **B (real code, evaluated separately - do NOT fold into this doc pass):** Blender F-09, F-11, F-35, F-39, F-44, F-53, F-55, F-63, F-71, F-79, F-81, F-89, F-94, F-101, F-102, F-108, F-109, F-113, F-115; Godot F-01, F-08, F-09, F-10, F-11, F-12, F-13, F-14, F-20; Photoshop F-03, F-04, F-05, F-18, F-19, F-20, F-21, F-23, F-24, F-25, F-28, F-29, F-30, F-36, F-38, F-39.
- **C (no action):** Blender F-21, F-29, F-30, F-42, F-43, F-45, F-61, F-62, F-73, F-80, F-107, F-114, F-116; Godot F-03; Photoshop F-01, F-02, F-08, F-13, F-14, F-26, F-35, F-37, F-40.

**Why it matters:** The doc pages and in-panel help describe an older, smaller surface than what ships - whole controls (status badges, `?` help buttons, atlas/region readouts, driver In/Out range, bundle-textures, import placement, the entire Photoshop Tags/Validate/Debug panels) are absent from the docs, and several doc sentences now state the opposite of what the code does (validation check list, "next to the .blend", "validates against the schema", driver axis "local rotation", weight-overlay "ships later"). A user reading the docs is misled, and the help-surfaces specs (023/036) are left half-done. Grouping avoids 94 micro-issues for what is one editorial pass per doc home.

**Scope sketch:** One pass per doc home, source-of-truth = the A rows in findings.md. (1) Blender `docs/02-blender-addon/*` - reconcile each panel page against its panel module, add the missing controls, fix the stale `drift` sentences (notably 09-validation.md's check list F-85/86/87/88, 10-pipeline.md F-95/96), and document the header badge/`?` convention once. (2) Godot `docs/04-godot-plugin/index.md` - document the `type:"mesh"` element, per-element fields, slot routing, track types, import options (F-04..F-19 A rows). (3) Photoshop `docs/03-photoshop-plugin/index.md` - document the Tags/Validate/Debug/Import/Migration sections and the PPU/template/forget controls (F-09..F-34 A rows), fix the v2/v1 comment drift (F-06/F-07). Also fix stale in-code help/description strings that are A (e.g. Blender F-12 PG axis labels, F-56 weight-overlay description, F-60 bind error string). Clear each finding's `(see finding)` pointer as its doc lands.

**Trigger to revisit:** the next help-surfaces or documentation spec (successor to 036/023); land per-subsystem so each doc home is reconciled in one sitting. The B set is tracked separately for a code-fix pass (see the session evaluation 2026-06-15); the C set is dismissed in findings.md.
