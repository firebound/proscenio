# Photoshop plugin backlog

Drained on 2026-06-20: every item here was routed into the focused specs of the backlog-drain wave (see [`_index.md`](_index.md)). This file is kept as a thin pointer so existing references resolve; recover the original entries from this file's git history.

Routed to:

- The data-loss and resilience items (spritesheet token rewrite, duplicate-sibling targeting, per-entry import guard, filename-template overwrite, dropped advanced-field input) and the minor staleness and race group -> spec 053 photoshop-data-integrity.
