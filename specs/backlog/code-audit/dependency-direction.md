# Dependency direction

Layering inversions: UI/panels reaching into operator-private state, and one `core/` module importing bpy at top against its own contract. All confirmed. Low/medium effort; the fix is always a public accessor or a relocation.

## Confirmed

- **panel-reads-automesh-internals** `[low/small]` `[confirmed]` - [mesh_generation.py:214](../../../apps/blender/panels/mesh_generation.py#L214) - `_draw_modal_indicator` couples directly to `PROSCENIO_OT_automesh_authoring` class internals (`_statusbar_appended`/`_current_stage_label`/`_current_active_tool`/`_current_stage`) and the private `_status_bar._TOOL_LABELS`, with no read-only accessor mediating. Fix: add a classmethod/module-level `authoring_modal_state()` returning a frozen dataclass (active, stage, stage_label, active_tool, tools+labels); have the panel indicator and `_draw_statusbar_authoring` both consume it.
- **panel-reads-quickarm-private** `[low/trivial]` `[confirmed]` `[quick win]` - [skeleton.py:801](../../../apps/blender/panels/skeleton.py#L801) - the panel reads operator-private `PROSCENIO_OT_quick_armature._modal_running` via getattr at lines 807 and 827; no public accessor exists. Fix: add `@classmethod is_running(cls) -> bool` (and optionally `draw_shortcuts(cls, layout)`) on the operator; call those instead of getattr on the private ClassVar. Note: the automesh subpanel (mesh_generation.py:232 reading `op._current_active_tool`) has the same gap if a uniform fix is wanted.
- **i18n-bpy-top-import** `[medium/small]` `[confirmed]` (found by phase-2 reconcile of the 16 un-inventoried files) - [i18n.py:22](../../../apps/blender/core/i18n.py#L22) - top-level `import bpy` in a direct child of `core/`, against the documented contract (core/__init__.py:6-11: direct children must be bpy-free or import bpy lazily inside a function; bpy-top-import modules belong under `core/bpy_helpers/`). Fix: move `i18n.py` under `core/bpy_helpers/` (or to the addon top level next to addon_prefs.py, since it is a register/unregister module), or convert to lazy per-function `import bpy`.
